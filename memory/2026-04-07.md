# 2026-04-07 记忆日志

---

## [18:49 补充] Bundle Patch 方案完成

### 确认情况
- 用户没有全局安装 `@anthropic-ai/claude-code`（`npm list -g` 返回 empty）
- `where.exe claude` 找不到
- 指导用户执行 `npm install -g @anthropic-ai/claude-code` 安装成功
- 安装路径确认：`C:\Users\benjamin\AppData\Roaming\npm\claude`
- Bundle 文件：`C:\Users\benjamin\AppData\Roaming\npm\node_modules\@anthropic-ai\claude-code\cli.js`
- Bundle 大小：12.6 MB（压缩单文件）
- 版本：`2.1.92`，Build time：`2026-04-03T23:25:15Z`

### Bundle 内关键函数名（混淆后）
- `CJ` = `isAnthropicAuthEnabled`
- `dT` = `getAnthropicApiKey`
- `j$` = `getAnthropicApiKeyWithSource`（内部辅助）

### Bundle Patch 策略
直接字符串替换 `cli.js`，无需重编译：

**Patch 1** — `isAnthropicAuthEnabled (CJ)`：
- 在函数入口加 `if(process.env.HUNYUAN_API_KEY)return!1;`

**Patch 2** — `getAnthropicApiKey (dT)`：
- 在函数入口加 `if(process.env.HUNYUAN_API_KEY)return process.env.HUNYUAN_API_KEY;`

**Patch 3** — `getAnthropicClient`（异步工厂函数）：
- 入口特征：`}):Promise<Anthropic>{const containerId=process.env.CLAUDE_CODE_CONTAINER_ID`
- 注入 `if(process.env.HUNYUAN_API_KEY)return createHunyuanClient();` 分支
- 备用特征：`Promise<Anthropic>{const containerId=process.env.CLAUDE_CODE_CONTAINER_ID`

**Patch 4** — 在文件开头注入完整 `createHunyuanClient` 函数
- 包含：常量定义、消息格式转换、工具转换、SSE流转换、主工厂函数
- 使用原生 `fetch` API，兼容 Node.js v22（无额外依赖）

### Patch 脚本位置
已生成完整 PowerShell 脚本，用户保存为 `C:\Users\benjamin\patch-claude.ps1`
运行方式：`powershell -ExecutionPolicy Bypass -File "C:\Users\benjamin\patch-claude.ps1"`

### 后续步骤（等待用户执行结果）
1. 用户运行 patch 脚本
2. 确认 Patch 1/2/4 成功，Patch 3 可能需要微调（空格/特征字符串差异）
3. 用户测试：
   ```powershell
   $env:HUNYUAN_API_KEY = "实际APIKey"
   $env:HUNYUAN_MODEL = "hunyuan-turbos-latest"
   claude
   ```
4. 如 Patch 3 失败，需要用户提供具体的 `getAnthropicClient` 附近原文来定位注入点

### 注意事项
- `cli.js.bak` 已备份，随时可恢复
- 若模型名不匹配（bundle 内有型号白名单检查），可能需要额外 patch 绕过模型校验
- 混元流式响应使用 `toolBlockIndex = tc.index + 1` 偏移计算，待实测验证

---

## [18:56 补充] 用户决定改用本地编译方案

- 用户决定放弃 bundle patch，改为**本地编译 TypeScript 源码**
- 计划：
  1. 安装 Bun（`powershell -c "irm bun.sh/install.ps1 | iex"`）
  2. 找回/补充 package.json（源码目录 `F:\Claude code src\src\` 缺少构建配置）
- 服务器端已完成的 TypeScript 改动（`claude-code-analysis/claude-code-source/`）可直接同步到用户本地使用
- **Bun 1.3.11 已安装成功**（2026-04-07 18:56）

## [18:58 关键发现] 本地编译方案不可行

**已安装包的 package.json 内容确认：**
- `dependencies: {}` — 所有依赖都打包进了 `cli.js`，无外部依赖
- `type: "module"`，入口 `bin.claude = "cli.js"`
- 仅有 `@img/sharp-*` 系列 optionalDependencies（图像处理）
- 没有 build 脚本，没有 tsconfig，没有 devDependencies

**结论：**
- 用户的 `F:\Claude code src\src\` 是反编译/提取出来的源码，**原始构建环境不完整**
- 无法用 `bun build` 重新编译（缺少完整的 tsconfig、依赖声明、构建配置）
- **回退到 bundle patch 方案是唯一可行路径**

**下次对话应直接引导用户回到 PowerShell patch 脚本方案：**
- 脚本路径：`C:\Users\benjamin\patch-claude.ps1`（已在本次对话中生成）
- 备份文件：`cli.js.bak`（需要先备份）
- 运行：`powershell -ExecutionPolicy Bypass -File "C:\Users\benjamin\patch-claude.ps1"`

---

## Claude Code 接入混元模型改造（进行中）

### 背景
Benjamin (Producer) 想把 Claude Code 源码改造，接入腾讯混元模型，并绕过 Anthropic 登录。

### 已完成的改动（在 /root/.openclaw/workspace/claude-code-analysis/claude-code-source/ 下）

**新建文件：**
- `services/api/hunyuan.ts` — 558行，核心适配层
  - 将 Anthropic SDK 调用格式转换为混元 OpenAI 兼容 API 格式
  - 包含 `convertMessages()`、`convertTools()`、`openAIStreamToAnthropicEvents()` 等
  - 导出 `createHunyuanClient()`、`isHunyuanEnabled()`、`HUNYUAN_DEFAULT_MODEL`、`HUNYUAN_SMALL_MODEL`

**修改文件：**
1. `utils/model/providers.ts`
   - `APIProvider` 类型加入 `'hunyuan'`
   - `getAPIProvider()` 加入混元优先判断：`process.env.HUNYUAN_API_KEY ? 'hunyuan' : ...`

2. `services/api/client.ts`
   - import `createHunyuanClient, isHunyuanEnabled` from `./hunyuan.js`
   - `getAnthropicClient()` 最开头加混元分支：`if (isHunyuanEnabled()) return createHunyuanClient() as Anthropic`

3. `utils/auth.ts`
   - `isAnthropicAuthEnabled()` 开头加：`if (process.env.HUNYUAN_API_KEY) return false`
   - `getAnthropicApiKey()` 开头加：`if (process.env.HUNYUAN_API_KEY) return process.env.HUNYUAN_API_KEY`

4. `utils/model/model.ts`
   - import `HUNYUAN_DEFAULT_MODEL, HUNYUAN_SMALL_MODEL` from hunyuan.ts
   - `getSmallFastModel()` 混元模式返回 `HUNYUAN_SMALL_MODEL`
   - `getDefaultSonnetModel()` 混元模式返回 `HUNYUAN_DEFAULT_MODEL`

### 当前卡点：无法编译

**用户环境：**
- Windows，路径：`F:\Claude code src\`
- 目录结构：只有一个 `src\` 子目录（纯源码，无 package.json / build 配置）
- 没有安装 Bun
- 之前说"编译过"实际上可能没真正成功

**下一步方向（下次对话继续）：**
- 检查用户是否有全局安装的 `@anthropic-ai/claude-code` npm 包
- 如果有：直接 patch 编译好的 bundle JS 文件（字符串替换，无需重新编译）
- 等待用户运行 `npm list -g @anthropic-ai/claude-code` 或 `where.exe claude` 的结果

### 混元环境变量说明（给用户的）
```
HUNYUAN_API_KEY=你的混元APIKey          # 必填，启用混元模式
HUNYUAN_MODEL=hunyuan-turbos-latest     # 主模型（默认）
HUNYUAN_SMALL_MODEL=hunyuan-lite        # 快速小模型（默认）
HUNYUAN_BASE_URL=https://api.hunyuan.cloud.tencent.com/v1  # 可选
```

### 可用混元模型
- `hunyuan-turbos-latest` — 主力推理，推荐
- `hunyuan-t1-latest` — 深度思考版
- `hunyuan-pro` — 稳定旗舰
- `hunyuan-lite` — 快速小模型
