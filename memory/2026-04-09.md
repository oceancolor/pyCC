# 2026-04-09 Memory

---

## Claude Code 源码本地编译（Windows，进行中）[14:07~15:07]

### 背景
Producer 想让 `F:\Claude code src` 目录里的 Claude Code 源码能在 Windows 本地编译（bun build），并接入混元大模型跑起来测试。

### 已确认的目录结构（用户本地）
```
F:\Claude code src\
├── src\            ← TypeScript 源码
├── stubs\          ← 已有但不完整
├── node_modules\   ← bun install 已跑过
├── scripts\
├── build.ts
├── bun.lock
├── global.d.ts     ← 已存在（11字节，但路径解析有问题）
├── package.json
├── setup-claude-code-build.ps1
└── tsconfig.json
```

### package.json 关键配置（已确认）
- `workspaces: ["packages/*", "packages/@ant/*"]`
- `dependencies` 中已用 `file:./stubs/ant-*` 指向本地 stub
- `overrides` 中也已配置相同的 stub 路径
- 已有 stubs：`ant-computer-use-mcp`, `ant-claude-for-chrome-mcp`, `ant-computer-use-swift`, `color-diff-napi`, `modifiers-napi`, `audio-capture-napi`
- **缺失**：`@ant/computer-use-input` 的 stub

### 当前报错（`bun run build.ts 2>&1 | Select-Object -First 80`）
1. `import '../global.d.ts'` 无法解析 → `ink/components/Box.tsx` 和 `ScrollBox.tsx`
2. `@ant/computer-use-input` 找不到（stubs 里没有，package.json 也没配置）
3. `@ant/computer-use-mcp/sentinelApps` 找不到（stub 不完整）
4. `@ant/computer-use-mcp/types` 找不到（stub 不完整）
5. `../services/contextCollapse/index.js` 找不到（该模块根本不在源码中，需要新建 stub）

### 服务器端源码分析结论
所有 `@ant/*` 包都是 Anthropic 私有包，公开源码里不存在。通过服务器上完整源码分析出了所有需要的类型和 API：

**需要补全/新建的 stubs：**

1. **`@ant/computer-use-input`**（完全缺失）
   - 需要：`ComputerUseInput`（type），`ComputerUseInputAPI`（type）
   - 运行时：只在 macOS 下实际用到，Windows 会 throw "not supported"

2. **`@ant/computer-use-mcp`**（stub 存在但不完整）
   - 缺 subpath exports: `/sentinelApps`, `/types`
   - 需导出：`API_RESIZE_PARAMS`, `targetImageSize`, `bindSessionContext`, `buildComputerUseTools`, `DEFAULT_GRANT_FLAGS`
   - `/types` 需导出：`CoordinateMode`, `CuSubGates`, `CuPermissionRequest`, `CuPermissionResponse`, `ScreenshotDims`
   - `/sentinelApps` 需导出：`getSentinelCategory`

3. **`@ant/claude-for-chrome-mcp`**（stub 存在但不完整）
   - 需导出：`BROWSER_TOOLS`（数组，包含 `name` 字段的对象列表）

4. **`services/contextCollapse/`**（完全缺失，需新建源码文件）
   - `index.ts` 需导出：`getStats()`, `subscribe()`, `isContextCollapseEnabled()`, `resetContextCollapse()`
   - `persist.ts` 需导出：`restoreFromEntries()`
   - `operations.ts`（被 context.tsx 用到）

5. **`global.d.ts`**（路径问题）
   - 文件在 `F:\Claude code src\global.d.ts`（根目录）
   - 被 `src/ink/components/Box.tsx` 和 `ScrollBox.tsx` 以 `../global.d.ts` 引入
   - 解析目标：`src/global.d.ts`（即 `global.d.ts` 需要也在 `src/` 下）
   - 修复方法：在 `src/` 下也放一份 `global.d.ts`，或修改这两个文件的 import 路径

### 下一步（等待继续）
需要给 Producer 生成完整的 PowerShell 脚本，一次性：
1. 补全所有缺失的 stub 文件内容
2. 在 `src/` 下创建 `global.d.ts`
3. 在 `src/services/contextCollapse/` 下创建三个模块文件
4. 更新 `package.json` 加入 `@ant/computer-use-input` stub 配置
5. 重新 `bun install` + `bun run build`

---


## Claude Code 移植进度快照 (14:30 GMT+8)

### 总文件数：400 .py（测试 48 passed ✅）

### 本日完成的批次
- **batch24**：slice_ansi, sanitization, undercover, exec_file_no_throw_portable, team_memory_ops, ca_certs_config, sleep, which, team_discovery, stream
- **batch26**（10/12 超时）：aws_auth_status_manager, telemetry_attributes, pdf_utils, get_worktree_paths, process_utils, browser, xdg, session_url, slash_command_parsing, semver
- **batch26 手写补完**：glob_patterns.py, horizontal_scroll.py
- **batch27 超时后手写**：collapse_hook_summaries, workload_context, sequential, collapse_teammate_shutdowns, tagged_id, idle_timeout, prompt_category, signal_utils, lockfile, timeouts, hyperlink, exec_sync_wrapper, semantic_number, semantic_boolean, modifiers, tempfile_utils, agent_swarms_enabled, unary_logging, profiler_base
- **最后一波超小文件**：set_utils, tempfile, signal, user_prompt_keywords, get_worktree_paths_portable, tool_schema_cache, ink_utils, standalone_agent, session_env_vars, peer_address, status_notice_helpers, auth_portable, classifier_approvals_hook, sinks, yaml_utils, immediate_command, with_resolvers, worktree_mode_enabled, user_agent, message_predicates, lazy_schema

### utils 覆盖率：~97%
剩余未覆盖（暂缓）：
- **大文件**：hooks(5022行), auth(2007行), worktree(1519行), ide(1494行), analyze_context(1382行)
- **图像类**：image_resizer(880), image_paste(416), logo_v2_utils(350), ansi_to_png(334), ansi_to_svg(272), fullscreen(202), heatmap(198), image_store(167), claude_desktop(152), apple_terminal_backup(124), image_validation(104), i_term_backup(73)
- **其他小文件**：json(277), glob(130)（已有 glob_patterns.py 覆盖部分）, process(68)（已有 process_utils.py）
- **图形组件**：ink(26) — 已有 ink_utils.py, yaml(15) — 已有 yaml_utils.py, set(53) — 已有 set_utils.py

### 关键路径
- Python工程根：`/root/.openclaw/workspace/claude-code-python/src/claude_code/`
- 源码：`/root/.openclaw/workspace/claude-code-analysis/claude-code-source/`
- PROGRESS.md：`/root/.openclaw/workspace/PROGRESS.md`

### 下一步建议
1. 攻克 `json.ts`(277行) → json_utils.py（jsonc-parser 逻辑）
2. 推进 tools 模块（当前 ~11% 覆盖）
3. 推进 services 模块（当前 ~14/128）
4. 推进 commands 模块（当前 14/110）
5. 大文件（hooks/auth/worktree/ide/analyze_context）分段移植

### 命名约定（已确立）
- glob.ts → glob_patterns.py（避免与标准库冲突）
- process.ts → process_utils.py
- signal.ts → signal.py（alias to signal_utils）
- tempfile.ts → tempfile.py（alias to tempfile_utils）
- set.ts → set_utils.py
- json.ts → json_utils.py（待写）
- ink.ts → ink_utils.py
- yaml.ts → yaml_utils.py
