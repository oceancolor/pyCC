# 2026-05-28 Daily Memory

## 端到端集成测试 + 工具补全 + 混元接入（全部完成）

### 测试进展
- 新增 `tests/test_integration.py`（42 cases）+ `tests/test_new_tools.py`（82 cases）
- 总测试数：**180 passed，0 failed**（原 48 → 180）
- 覆盖：CoreTools / QueryEngine / CostTracker / History / Commands / E2E Pipeline / ImportHealth

### FileMoveTool / NotebookReadTool / Task*Tool 补全
- 7 个工具从抽象类（有 `run()` 但缺 `async call()`）补全为完整实现
- `file_move_tool.py`：移动/重命名文件目录，支持 overwrite、自动创建父目录
- `notebook_read_tool.py`：读取 .ipynb，支持 code/markdown cells，stream/execute_result/error 输出
- `task_tool.py`：TaskCreateTool / TaskGetTool / TaskListTool / TaskUpdateTool / TaskStopTool
  - 内置 TaskRecord / TaskStatus / 进程内 _TASK_STORE
  - 完整生命周期：create→get→update→stop

### get_commands() bug 修复
- 根因：`is_command_enabled()` 直接访问 `cmd.is_enabled`，但部分 command 是独立 dataclass 没有该字段
- 修复：`types/command.py` 改用 `getattr(cmd, 'is_enabled', None)`
- 同步修复：`commands/__init__.py` fallback 函数（原来用的是 `enabled` 字段，错误）
- 补全：`commands/model/index.py` 加 `default = model_cmd`，让 model 命令可被加载
- 效果：`get_commands()` 从崩溃 → 正常返回 57 个命令

### 混元 API 接入（hunyuan-turbos-latest）
- `client.py` 新增 `_HunyuanClient` + `_HunyuanMessages` + `_HunyuanResponse` + `_UsageObj`
  - OpenAI-compat wrapper，让 `client.messages.create(**anthropic_kwargs)` 无缝走混元
  - Anthropic messages 格式 ↔ OpenAI chat/completions 双向转换
  - tool_use / tool_calls 双向转换完整实现
- Bug 修复 1：`query.py` 序列化 tools 时 `getattr(t, "description")` 拿到 async bound method → JSON 崩溃
  - 修复：`_run_turn` 中 await 所有 tool 的 async 方法，预先序列化为 dict
- Bug 修复 2：tool dispatch 大小写不匹配（混元返回 "Bash"，工具注册为 "bash"）
  - 修复：查找时大小写不敏感，同时支持 dict（序列化后）和 Tool 对象

### 混元实测结果
- API key：`sk-jBtg7VFw...`（Producer 提供，2026-05-28）
- 模型：`hunyuan-turbos-latest`（快速），`hunyuan-t1-latest`（推理）
- `hunyuan-think` 和 `hy3-preview` 均不存在，API 不认
- Step 1 直接 HTTP ✅
- Step 2 _HunyuanClient wrapper ✅
- Step 3 tool_use 完整循环 ✅（BashTool 执行 echo，模型读到结果）
- Step 4 多轮对话 ✅（4 条历史消息正常累积）

### 关键文件路径
- `src/claude_code/services/api/client.py` — 混元 wrapper
- `src/claude_code/tools/file_move_tool.py` — 补全
- `src/claude_code/tools/notebook_read_tool.py` — 补全
- `src/claude_code/tools/task_tool.py` — 5 工具合并补全
- `src/claude_code/types/command.py` — is_enabled bug 修复
- `scripts/smoke_test_hunyuan.py` — 4步冒烟测试脚本
- `tests/test_integration.py` — 集成测试
- `tests/test_new_tools.py` — 新工具测试

### 下一步（未完成）
- 真实 API 完整冒烟（用 hunyuan-t1-latest 跑复杂推理任务验证）
- cli/print.py 部分 React/Ink 渲染逻辑待细化
- 导入测试（import all modules 验证无循环依赖）
